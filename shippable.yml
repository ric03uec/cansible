language: python

env:
  - HUB_REPO=ric03uec/cansible

build:
  ci:
    - export CANSIBLE_IMAGE="$HUB_REPO:$BUILD_NUMBER"
    - echo $CANSIBLE_IMAGE

branches:
  only:
    - devel
    - master
    - release
