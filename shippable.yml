language: python

env:
  - HUB_REPO=ric03uec/cansible

build:
  ci:
    - make
    - make build

integrations:
  hub:
    - integrationName: ric03uec-dockerhub
      type: dockerRegistryLogin

branches:
  only:
    - devel
    - master
