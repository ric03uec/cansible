language: python

env:
  - HUB_REPO=ric03uec/cansible

build:
  ci:
    - make
    - make build

integrations:
  hub:
    - integrationName: ric03uec-dockerhub
      type: docker

branches:
  only:
    - devel
    - master
